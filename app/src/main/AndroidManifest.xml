<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapss">

    <!-- Camera & Storage -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Correct for Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Legacy (ignored on Android 11+) but safe to keep -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApss">

<!--        android:networkSecurityConfig="@xml/network_security_config"-->
<!--        android:usesCleartextTraffic="true"-->

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:usesCleartextTraffic="true"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Correct Login Activity -->
        <activity
            android:name=".activities.Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Correct Main Activity -->
        <activity
            android:name=".activities.Main"
            android:exported="false"
            android:launchMode="singleTop" />

        <!-- Page 1, Page 2, Page 3 -->
        <activity android:name=".activities.Page1" android:launchMode="standard" android:exported="false" />
        <activity android:name=".activities.Page2" android:launchMode="standard" android:exported="false" />
        <activity android:name=".activities.Page3" android:launchMode="standard" android:exported="false" />

    </application>

</manifest>